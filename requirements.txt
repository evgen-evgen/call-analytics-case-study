
httpx>=0.27,<1.0
faster-whisper>=1.1,<2.0
pyannote.audio>=4.0,<5.0
pydantic>=2.0,<3.0
# The base Pipelines image currently ships torch 2.8 CPU. Newer TorchCodec
# wheels target newer torch/CUDA runtimes and fail looking for libnvrtc.so.13.
torchcodec>=0.6,<0.8
# Keep the OpenCV dependency already present in the base image compatible.
numpy>=2,<2.3
openai>=1.68.0